"use client";

import { useState } from "react";

export default function Home() {
  const [accepted, setAccepted] = useState(false);
  const [day, setDay] = useState("");

  return (
    <main className="min-h-screen bg-gradient-to-br from-pink-100 via-rose-50 to-purple-100 flex items-center justify-center px-5 py-10">
      <div className="relative w-full max-w-md">
        {/* Decorative hearts */}
        <div className="absolute -top-8 -left-4 text-3xl animate-bounce">
          💗
        </div>
        <div className="absolute -top-5 -right-3 text-2xl animate-pulse">
          ✨
        </div>

        <div className="rounded-[2rem] bg-white/80 backdrop-blur-xl shadow-2xl p-8 text-center border border-white">
          {!accepted ? (
            <>
              <div className="text-6xl mb-5">🌹</div>

              <p className="text-sm font-medium text-rose-500 mb-2">
                Надад нэг санал байна...
              </p>

              <h1 className="text-3xl font-bold text-gray-800 mb-5">
                Надтай болзоонд явах уу? ❤️
              </h1>

              <p className="text-gray-600 leading-7 mb-7">
                Энэ долоо хоногт нэг оройг надад гаргаарай 😉
                <br />
                Чамайг гоё газар дагуулаад,
                <br />
                хамт цаг өнгөрөөмөөр байна.
              </p>

              <div className="mb-6">
                <p className="text-sm text-gray-500 mb-3">
                  Аль өдөр чамд тохирох вэ?
                </p>

                <div className="grid grid-cols-2 gap-3">
                  <button
                    onClick={() => setDay("Пүрэв")}
                    className={`rounded-xl py-3 font-semibold transition ${
                      day === "Пүрэв"
                        ? "bg-rose-500 text-white shadow-lg scale-105"
                        : "bg-rose-50 text-rose-600 hover:bg-rose-100"
                    }`}
                  >
                    🌸 Пүрэв
                  </button>

                  <button
                    onClick={() => setDay("Баасан")}
                    className={`rounded-xl py-3 font-semibold transition ${
                      day === "Баасан"
                        ? "bg-rose-500 text-white shadow-lg scale-105"
                        : "bg-rose-50 text-rose-600 hover:bg-rose-100"
                    }`}
                  >
                    🌙 Баасан
                  </button>
                </div>
              </div>

              <button
                disabled={!day}
                onClick={() => setAccepted(true)}
                className={`w-full rounded-2xl py-4 font-bold text-lg transition ${
                  day
                    ? "bg-gradient-to-r from-rose-500 to-pink-500 text-white shadow-lg hover:scale-[1.02] active:scale-95"
                    : "bg-gray-200 text-gray-400 cursor-not-allowed"
                }`}
              >
                {day ? "Тийм ээ, явъя ❤️" : "Өдрөө эхлээд сонгоорой"}
              </button>

              <p className="text-xs text-gray-400 mt-5">
                Жижигхэн урилга, гэхдээ чин сэтгэлээсээ 💌
              </p>
            </>
          ) : (
            <div className="py-8">
              <div className="text-7xl mb-6 animate-bounce">🥰</div>

              <h1 className="text-4xl font-bold text-rose-500 mb-4">
                Yesss! ❤️
              </h1>

              <p className="text-xl text-gray-700 mb-3">
                Тэгвэл {day} уулзъя!
              </p>

              <p className="text-gray-500 leading-7 mb-7">
                Чамтай хамт гоё орой өнгөрөөхийг
                <br />
                тэсэн ядан хүлээж байна. ✨
              </p>

              <div className="rounded-2xl bg-rose-50 p-5">
                <p className="text-2xl mb-2">🌹 ☕ 🌙</p>
                <p className="font-semibold text-rose-600">
                  {day} — бидний жижигхэн болзоо
                </p>
              </div>

              <p className="text-xs text-gray-400 mt-6">
                See you soon ❤️
              </p>
            </div>
          )}
        </div>
      </div>
    </main>
  );
}